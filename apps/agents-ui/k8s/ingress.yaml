apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: agents-ui-ingress
  annotations:
    kubernetes.io/ingress.class: "nginx"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  rules:
  - host: agents-ui.dalaillama.in
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: agents-ui-svc
            port:
              number: 80
  tls:
  - hosts:
    - agents-ui.dalaillama.in
    secretName: agents-ui-tls
