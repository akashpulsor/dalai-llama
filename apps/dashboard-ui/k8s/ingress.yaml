apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dashboard-ui-ingress
  annotations:
    kubernetes.io/ingress.class: "nginx"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  rules:
  - host: dashboard-ui.dalaillama.in
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: dashboard-ui-svc
            port:
              number: 80
  tls:
  - hosts:
    - dashboard-ui.dalaillama.in
    secretName: dashboard-ui-tls
