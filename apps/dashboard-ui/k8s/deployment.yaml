apiVersion: apps/v1
kind: Deployment
metadata:
  name: dashboard-ui-deployment
  labels:
    app: dashboard-ui
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dashboard-ui
  template:
    metadata:
      labels:
        app: dashboard-ui
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/path: "/health"
        prometheus.io/port: "80"
    spec:
      containers:
      - name: dashboard-ui
        image: dalaillama/dashboard-ui:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 80
        envFrom:
        - configMapRef:
            name: dashboard-ui-config
